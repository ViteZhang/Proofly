-- =============================================================
-- Proofly · 19 Step 6.7 投递版本冻结
--
-- 标记已投递的那一刻，版本要固化成一份自己的副本：块写进
-- resume_blocks（resume_version_id 填、source_block_id 指回基线块），
-- 个人定位段没有块可挂，所以单独存一列。
--
-- 为什么必须固化：投出去的那份是什么样，事后必须还查得到 ——
-- 那是面试前唯一能复习的东西。基线之后怎么改，都不该动它。
-- =============================================================

alter table resume_versions
  add column if not exists headline text;
