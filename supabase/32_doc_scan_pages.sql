-- =============================================================
-- Proofly · 32 记下扫描件页数
--
-- 解析的标价里有一项是「扫描件每页 1 分」，而结算发生在几分钟后的
-- 另一次执行里 —— 那时候原始文件早就不在手上了。所以上传时把页数
-- 记下来，预估与结算读同一个数，不会一处算 18 页一处算 0 页。
-- =============================================================

alter table source_docs
  add column if not exists scan_pages int not null default 0;
